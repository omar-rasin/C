/* Write a C program to swap two numbers using a temporary variable. */
#include<stdio.h>
int main(){
   int x = 100;
   int y = 200;
    printf("After swapping the initial integers of 100,200 becomes %d , %d", y,x);
    return 0;
}